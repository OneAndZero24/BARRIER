# ============================================================================
# wandb bayesian sweep -- DDPM CIFAR-10 InTAct ESD (match-to-uncond)  (50 runs)
# ============================================================================
# Usage:
#   cd DDPM
#   wandb agent $(wandb sweep configs/sweep_esd_bayes.yaml 2>/dev/null | grep -o 'wandb agent .*') --count 50
# ============================================================================

program: pipeline.py
method: bayes
entity: oneandzero24
project: intact-ddpm
name: esd-bayes-50

metric:
  name: score
  goal: maximize

early_terminate:
  type: hyperband
  min_iter: 10
  eta: 3

command:
  - ${env}
  - python
  - ${program}
  - --config
  - configs/pipeline.yaml

parameters:
  unlearn.method:
    value: "esd"

  unlearn.lr:
    distribution: log_uniform_values
    min: 1e-4
    max: 3e-2

  unlearn.n_iters:
    values: [2000, 5000, 10000, 15000]

  unlearn.batch_size:
    values: [32, 64, 128]

  unlearn.alpha:
    value: 0.0

  training.esd_frozen_uncond:
    values: [true, false]

  intact.lambda_interval:
    distribution: log_uniform_values
    min: 0.001
    max: 0.5

  intact.reduced_dim:
    values: [8, 16, 32]

  intact.normalize_protection:
    values: [true, false]

  intact.use_actual_bounds:
    values: [true, false]
